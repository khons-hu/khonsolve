# Thinkroom implementation
Approved: user said go on; JavaScript runner plus any-language notes selected as recommended default.

1. Original content: five tracks, 15 exercises, progressive hints, concrete self-review rubrics and worked explanations. Coding exercises include public tests and reference functions for verification.
2. Static application: searchable library, track filters, focus workspace, local drafts, hints, review/reflection, revisit queue, validated JSON backup import/export, theme preference.
3. Execution: JavaScript worker inside opaque-origin sandbox iframe; block network via CSP, terminate slow runs. No server execution and no runtime dependencies.
4. Verification: Node tests for exercise references, persistence validation and runner success/error; browser verify timeout, sandbox, navigation, persistence, hints, review, themes and narrow viewport.
5. Publish: user explicitly authorized public GitHub, Vercel deployment and portfolio listing. Push only project files after checks; verify live and add source/live links to portfolio.
